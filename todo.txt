bundle exec jekyll build
mv _site ../_site_backup
git checkout gh-pages
git rm -rf .
cp -r ../_site_backup/* .
git add .
git commit -m "Fix baseurl issue"
git push origin gh-pages --force
git checkout main
rm -rf ../_site_backup
